<p>
  <h4><%= t(:add_more_friends) %></h4>
  <%= form_tag(friendships_path, method: "post") do %>
    <%= collection_check_boxes(:user, :friend_ids, @nonfriends, :id, :email)%>
    <p>
      <%= submit_tag(t(:add_friends))%>
    </p>
  <% end %>
</p>
